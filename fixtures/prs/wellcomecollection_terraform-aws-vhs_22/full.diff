diff --git a/RELEASE.md b/RELEASE.md
new file mode 100644
index 0000000..4ad8830
--- /dev/null
+++ b/RELEASE.md
@@ -0,0 +1,8 @@
+RELEASE_TYPE: minor
+
+Add `deletion_protection_enabled` and `point_in_time_recovery_enabled` variables
+to both stores, so a VHS table can be protected against accidental deletion and
+restored to a point in time.
+
+Both default to `false`, which is what the module did before, so existing
+callers see no change.
diff --git a/multi-version-store/dynamo.tf b/multi-version-store/dynamo.tf
index ef3648b..95b66ff 100644
--- a/multi-version-store/dynamo.tf
+++ b/multi-version-store/dynamo.tf
@@ -7,6 +7,12 @@ resource "aws_dynamodb_table" "table" {
 
   billing_mode = "PAY_PER_REQUEST"
 
+  deletion_protection_enabled = var.deletion_protection_enabled
+
+  point_in_time_recovery {
+    enabled = var.point_in_time_recovery_enabled
+  }
+
   attribute {
     name = "id"
     type = "S"
diff --git a/multi-version-store/variables.tf b/multi-version-store/variables.tf
index 17f8ab3..59f31da 100644
--- a/multi-version-store/variables.tf
+++ b/multi-version-store/variables.tf
@@ -43,3 +43,17 @@ variable "cycle_objects_to_standard_ia" {
   type    = bool
   default = true
 }
+
+variable "deletion_protection_enabled" {
+  description = "Whether DynamoDB deletion protection is enabled on the table"
+
+  type    = bool
+  default = false
+}
+
+variable "point_in_time_recovery_enabled" {
+  description = "Whether DynamoDB point-in-time recovery is enabled on the table"
+
+  type    = bool
+  default = false
+}
diff --git a/single-version-store/dynamo.tf b/single-version-store/dynamo.tf
index b66cc9e..4527b95 100644
--- a/single-version-store/dynamo.tf
+++ b/single-version-store/dynamo.tf
@@ -6,6 +6,12 @@ resource "aws_dynamodb_table" "table" {
 
   billing_mode = "PAY_PER_REQUEST"
 
+  deletion_protection_enabled = var.deletion_protection_enabled
+
+  point_in_time_recovery {
+    enabled = var.point_in_time_recovery_enabled
+  }
+
   attribute {
     name = "id"
     type = "S"
diff --git a/single-version-store/variables.tf b/single-version-store/variables.tf
index 17f8ab3..59f31da 100644
--- a/single-version-store/variables.tf
+++ b/single-version-store/variables.tf
@@ -43,3 +43,17 @@ variable "cycle_objects_to_standard_ia" {
   type    = bool
   default = true
 }
+
+variable "deletion_protection_enabled" {
+  description = "Whether DynamoDB deletion protection is enabled on the table"
+
+  type    = bool
+  default = false
+}
+
+variable "point_in_time_recovery_enabled" {
+  description = "Whether DynamoDB point-in-time recovery is enabled on the table"
+
+  type    = bool
+  default = false
+}
